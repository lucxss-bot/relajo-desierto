---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`rounded-lg border border-border bg-surface p-6 shadow-sm ${className || ""}`}>
  <slot />
</div>
